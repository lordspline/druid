apiVersion: batch/v1
kind: Job
metadata:
  name: "id-3e70afe5cd823dfc7dd308eea616426b"
  labels:
    robux.k8s.peons: "true"
    robux.task.id: "id"
    robux.task.type: "noop"
    robux.task.group.id: "id"
    robux.task.datasource: "datasource"
    robux.overlord.namespace: "namespace"
  annotations:
    task.id: "id"
    task.type: "noop"
    task.group.id: "id"
    task.datasource: "datasource"
    task.jobTemplate: base
spec:
  activeDeadlineSeconds: 14400
  backoffLimit: 0
  ttlSecondsAfterFinished: 172800
  template:
    metadata:
      labels:
        robux.k8s.peons: "true"
        robux.task.id: "id"
        robux.task.type: "noop"
        robux.task.group.id: "id"
        robux.task.datasource: "datasource"
        robux.overlord.namespace: "namespace"
      annotations:
        tls.enabled: "false"
        task.id: "id"
        task.type: "noop"
        task.group.id: "id"
        task.datasource: "datasource"
        task.jobTemplate: base
    spec:
      containers:
        - command:
            - sleep
            - "3600"
          env:
            - name: "TASK_DIR"
              value: "/tmp"
            - name: "TASK_ID"
              value: "id"
            - name: "LOAD_BROADCAST_DATASOURCE_MODE"
              value: "ALL"
            - name: "LOAD_BROADCAST_SEGMENTS"
              value: "false"
          image: one
          name: primary
